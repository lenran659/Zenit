# Zenit项目管理 MVP需求
1. 登录模块 (Auth) —— 参考 Jenkins/Linear
交互逻辑： 极简表单，背景采用深色微渐变。、
功能点：
子部署标识： 登录页底部显示当前私有节点的 ID 和运行状态。

2. 项目登记页面 (Project Hub)
2.1 项目列表视图： 采用紧凑型列表，显示项目图标、进度条、负责人、活跃成员。
2.2 项目配置： * 新建项目（设置 Key、名称、描述）。
修改/删除（需二次确认或输入项目 Key 确认，防止误删）。

3. 任务与敏捷管理 (Issue & Agile)
这是核心页面，采用 “多维视图切换” 逻辑。
3.1 任务登记 (Create Issue)：
全局快捷键： 按 C 弹出弹窗，支持 Markdown 描述。
关联性： 强制选择所属项目，可选关联“迭代”或“缺陷”。

3.2 视图展示 (Views)：
表格 (List)： 极致响应，支持列排序和过滤（优先级、状态、负责人）。
看板 (Kanban)： 经典的拖拽流转（Todo -> In Progress -> Done）。
甘特图 (Timeline)： 以周为单位，展示任务在迭代周期内的覆盖情况。

4. 人员分配模块 (People & Teams)
4.1 项目角色：
负责人 (Owner)： 拥有最高权限，控制项目设置。
参与人 (Members)： 拥有读写权限。
4.2 任务指派 (Assignee)：
负责人： 任务的主办人（单选）。
参与人/关注者： 抄送通知的对象（多选）。

5. 迭代模块 (Cycles/Sprints)
5.1 周期管理： 定义迭代的开始与结束日期（通常为 1-2 周）。

5.2 任务规划： * Backlog： 未分配的任务池。

Active Cycle： 当前正在进行的任务。

自动统计当前迭代的完成率。

6. 缺陷模块 (Bugs/Issues)
在 Zenit 中，我们不把 Bug 做成独立系统，而是作为 Issue 的一种类型，以保持“小而美”。

6.1 Bug 生命周期： * 类型标识为 Bug，显示醒目的红色图标。

修复流程： 新建 -> 修复中 -> 已解决 -> 验证关闭。

6.2 负责人联动： 自动指派给项目负责人或模块开发人员。